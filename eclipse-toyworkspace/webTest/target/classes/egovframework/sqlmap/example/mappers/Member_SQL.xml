<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.example.login.service.impl.MemberMapper">

	
	<select id="detail" parameterType="String" resultType="egovframework.example.login.service.MemberVO">
		SELECT 
			MEM_ID
			, MEM_NAME
			, MEM_PASS
			, MEM_EMAIL
			, MEM_TEL
			, MEM_ADDR
			, MEM_ADDR2
			, MEM_DEL_YN
			, MEM_ROLE
			, MEM_BIRTH
		FROM
			MEMBER
		WHERE 
			1=1
		AND MEM_ID=#{memId}		
	</select>
	
	<insert id="add" parameterType="memberVO">
		INSERT INTO
			MEMBER
				( MEM_ID
				, MEM_NAME
				, MEM_PASS
				, MEM_EMAIL
				, MEM_TEL
				, MEM_ADDR
				, MEM_ADDR2
				, MEM_DEL_YN
				, MEM_ROLE				
				)	
		VALUES
			( #{memId}
			 ,#{memName}
			 ,#{memPass}	
			 ,#{memEmail}	
			 ,#{memTel}	
			 ,#{memAddr}	
			 ,#{memAddr2}	
			 ,'N'	
			 ,'ROLE_USER'		
			)
	
	</insert>
	
	
	<select id="idCheck" parameterType="String" resultType="memberVO">
		SELECT 
			MEM_ID
			, MEM_NAME
			, MEM_PASS
			, MEM_EMAIL
			, MEM_TEL
			, MEM_ADDR
			, MEM_ADDR2
			, MEM_DEL_YN
			, MEM_ROLE
			, MEM_BIRTH
		FROM
			MEMBER
		WHERE 
			1=1
		AND MEM_ID=#{memId}			
	</select>
	
	
</mapper>